# 🚀 AgroAI - Быстрый старт

## Пошаговая инструкция для запуска на компьютере

### Шаг 1: Подготовка Python окружения ✅

Вы уже установили **Python 3.12.7** и **PyCharm** - отлично!

### Шаг 2: Создание структуры проекта

Создайте следующую структуру папок в PyCharm:

```
AgroAI/                      # Главная папка проекта
│
├── main.py                  # Главный файл
├── config.py                # Конфигурация
├── requirements.txt         # Зависимости
├── .env                     # API ключи
├── README.md               # Документация
│
├── modules/                 # Папка с модулями
│   ├── __init__.py
│   ├── database.py
│   ├── ndvi_analyzer.py
│   ├── plant_analyzer.py
│   ├── credit_analyzer.py
│   └── ai_advisor.py
│
├── data/                    # Создастся автоматически
├── temp/                    # Создастся автоматически
└── logs/                    # Создастся автоматически
```

### Шаг 3: Установка библиотек

Откройте терминал в PyCharm (Alt+F12) и выполните:

```bash
# Установка всех зависимостей
pip install python-telegram-bot==20.7 aiohttp numpy pillow anthropic python-dotenv requests colorlog pydantic
```

### Шаг 4: Получение API ключей

#### 4.1 Telegram Bot Token (ОБЯЗАТЕЛЬНО)

1. Откройте Telegram на телефоне
2. Найдите бота **@BotFather**
3. Отправьте команду: `/newbot`
4. Введите имя бота: `AgroAI Assistant`
5. Введите username: `your_agroai_bot` (должен заканчиваться на `_bot`)
6. **Скопируйте полученный токен** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 4.2 Anthropic API Key (ОБЯЗАТЕЛЬНО для AI функций)

1. Зайдите на [console.anthropic.com](https://console.anthropic.com/)
2. Зарегистрируйтесь
3. Перейдите в **API Keys**
4. Нажмите **Create Key**
5. **Скопируйте ключ** (начинается с `sk-ant-`)

#### 4.3 Sentinel Hub (ОПЦИОНАЛЬНО - можно использовать демо-режим)

Для работы в демо-режиме просто поставьте любое значение: `demo_key_12345`

### Шаг 5: Настройка config.py

Откройте `config.py` и замените значения:

```python
# Замените на ВАШИ ключи:
BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"  # Ваш Telegram токен
ANTHROPIC_API_KEY = "sk-ant-api03-xxxxxxxxxxxxx"     # Ваш Claude AI ключ
SATELLITE_API_KEY = "demo_key_12345"                  # Можно оставить для демо
```

### Шаг 6: Запуск бота

#### Вариант 1: Через PyCharm

1. Откройте файл `main.py`
2. Нажмите зеленую кнопку **▶ Run** (или Shift+F10)
3. В консоли должно появиться: `🚀 AgroAI Bot запущен!`

#### Вариант 2: Через терминал

```bash
python main.py
```

### Шаг 7: Тестирование бота

1. Откройте Telegram
2. Найдите своего бота по username (который вы создали)
3. Нажмите **START** или отправьте `/start`
4. Выберите язык (Узбекский или Русский)
5. Попробуйте функции:
   - 🛰 **NDVI Анализ**: Отправьте координаты `41.2995, 69.2401`
   - 🌱 **Анализ растений**: Отправьте фото любого растения
   - 💳 **Кредитный анализ**: Введите данные
   - 💡 **AI Советы**: Выберите категорию

## 🎯 Проверка работы

### Успешный запуск выглядит так:

```
✅ Конфигурация загружена: AgroAI v1.0.0
✅ База данных инициализирована
🚀 AgroAI Bot запущен!
```

### Если появляются ошибки:

#### Ошибка: `ModuleNotFoundError: No module named 'telegram'`
```bash
pip install python-telegram-bot==20.7
```

#### Ошибка: `ModuleNotFoundError: No module named 'anthropic'`
```bash
pip install anthropic
```

#### Ошибка: `Invalid token`
- Проверьте правильность токена в `config.py`
- Убедитесь, что токен скопирован полностью
- Проверьте, что нет лишних пробелов

#### Ошибка: `Rate limit exceeded` (Anthropic)
- Проверьте баланс на [console.anthropic.com](https://console.anthropic.com/)
- Бот временно работает в демо-режиме для анализа растений

## 📊 Демо-режим vs Полный режим

### Демо-режим (работает БЕЗ внешних API)
- ✅ Основной интерфейс бота
- ✅ База данных
- ✅ Симуляция NDVI данных
- ✅ Кредитный анализ
- ✅ Базовые советы

### Полный режим (требуются API ключи)
- ✅ Реальные спутниковые данные (Sentinel Hub)
- ✅ AI анализ растений по фото (Claude Vision)
- ✅ Продвинутые AI советы (Claude)
- ✅ Интеграция с банками

## 🔧 Настройка для продакшена

### 1. Использование .env файла (рекомендуется)

Создайте файл `.env`:
```bash
BOT_TOKEN=your_token_here
ANTHROPIC_API_KEY=your_key_here
SATELLITE_API_KEY=your_key_here
```

Измените `config.py`:
```python
from dotenv import load_dotenv
import os

load_dotenv()

BOT_TOKEN = os.getenv("BOT_TOKEN")
ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY")
SATELLITE_API_KEY = os.getenv("SATELLITE_API_KEY")
```

### 2. Автозапуск при загрузке Windows

1. Создайте файл `start_agroai.bat`:
```batch
@echo off
cd C:\путь\к\AgroAI
python main.py
pause
```

2. Скопируйте ярлык в папку автозагрузки:
   - Нажмите `Win+R`
   - Введите `shell:startup`
   - Скопируйте ярлык `.bat` файла

### 3. Логирование

Все логи сохраняются в `logs/agroai.log`

Просмотр в реальном времени:
```bash
# Windows PowerShell
Get-Content logs\agroai.log -Wait -Tail 50
```

## 💡 Советы по использованию

### Для фермеров:

1. **NDVI Анализ**:
   - Используйте координаты центра вашего поля
   - Проводите анализ раз в неделю
   - Следите за трендом NDVI

2. **Анализ растений**:
   - Фотографируйте при хорошем освещении
   - Делайте фото крупным планом
   - Снимайте пораженные участки

3. **Кредитный анализ**:
   - Вносите точные данные
   - Обновляйте информацию каждый сезон
   - Следуйте рекомендациям для улучшения рейтинга

### Для разработчиков:

1. **Кастомизация**:
   - Добавьте новые культуры в базу данных
   - Настройте под местный климат
   - Интегрируйте с местными банками

2. **Масштабирование**:
   - Используйте PostgreSQL вместо SQLite
   - Добавьте Redis для кеширования
   - Настройте webhook вместо polling

## 📞 Поддержка

Если что-то не работает:

1. Проверьте версию Python: `python --version` (должно быть 3.12.7)
2. Проверьте установку библиотек: `pip list`
3. Посмотрите логи: `logs/agroai.log`
4. Перезапустите бота

---

**Готово! Ваш AgroAI бот готов к работе! 🎉**

Следующие шаги:
- 📱 Протестируйте все функции
- 🌾 Пригласите фермеров использовать бота
- 📊 Анализируйте статистику использования
- 🚀 Добавляйте новые функции по запросу пользователей